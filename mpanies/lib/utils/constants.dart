import 'package:flutter/material.dart';

// color themes

const banner2 = Color(0xFFF4DBD6);


//const
//margin from the top
const bmargintop = EdgeInsets.only(top: 50);
//padding on all sides
const bpadding = EdgeInsets.all(30);
//styling for headings
const bheadings =
      TextStyle(color: Colors.black,fontSize: 24.0,fontWeight: FontWeight.bold);
const fheadings =
      TextStyle(color: Colors.white,fontSize: 24.0,fontWeight: FontWeight.bold);
const fsubheadings = TextStyle(color: Colors.white,fontSize: 14.0);

const buttonheadings =
    TextStyle(color: Colors.white, fontWeight: FontWeight.bold, fontSize: 20);
const subheadings =
    TextStyle(color: Colors.black, fontWeight: FontWeight.bold, fontSize: 20);

const dashcolor = Color(0xFFF09224);

const formheadings =
    TextStyle(color: Colors.black, fontWeight: FontWeight.w600, fontSize: 15);
